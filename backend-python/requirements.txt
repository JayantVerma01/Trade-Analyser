# Install with: pip install -r requirements.txt
# (--pre no longer required — all pinned versions are stable releases)

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary>=2.9.12,<3
pgvector==0.3.6

# AI / LangChain / LangGraph
# Upgraded to 0.3 line: required because pandas-ta-classic needs numpy>=2,
# and only langchain >=0.3 supports numpy 2.x. Our code's imports
# (langchain.schema, langchain_openai, langchain_core.tools, langgraph.prebuilt)
# are stable across 0.2 → 0.3 — no code changes needed.
langchain==0.3.13
langchain-openai==0.2.14
langchain-community==0.3.13
langchain-text-splitters==0.3.4
langgraph==0.2.62
openai==1.58.1

# PDF processing
pypdf==5.1.0
# PyMuPDF gives reliable image extraction + page rasterisation for vision analysis
pymupdf==1.24.14
Pillow==11.0.0

# Data / Indicators (numpy 2.x line — required by pandas-ta-classic)
pandas==2.2.3
numpy==2.1.3
# The original pandas-ta is abandoned (repo deleted, PyPI yanked).
# pandas-ta-classic is the maintained successor.
# NOTE: import name is `pandas_ta_classic` (NOT pandas_ta).
# Our indicator files use `import pandas_ta_classic as ta`.
pandas-ta-classic==0.3.15

# Utilities
python-jose[cryptography]==3.3.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
httpx==0.28.1
tenacity==9.0.0
tavily-python==0.5.0

# Free real market data via Yahoo Finance (no API key needed).
# Handles NSE (.NS) and BSE (.BO) suffixes automatically.
yfinance==1.5.1
